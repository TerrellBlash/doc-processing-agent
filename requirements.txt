# Core LangGraph + LangChain
langgraph>=0.4.0
langchain-openai>=0.3.0
langchain-core>=0.3.0

# API Server
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.0.0

# Azure deployment (swap MemorySaver for persistent storage)
# langgraph-checkpoint-postgres>=0.1.0   # uncomment for production
# asyncpg>=0.30.0                        # uncomment for production

# Testing
pytest>=8.0.0
httpx>=0.28.0  # for TestClient async support
